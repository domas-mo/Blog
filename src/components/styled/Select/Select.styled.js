import styled from "styled-components";

const StyledSelect = styled.form`
    margin: 2rem;

    select {
        position: relative;
        font-family: 'Open Sans', 'Helvetica Neue', 'Segoe UI', 'Calibri', 'Arial', sans-serif;
        font-size: 18px;
        color: #60666d;
        border: 2px solid #4e4a7e;
        border-radius: 10px;
        padding: 0 10px;
    }

    label {
        font-family: 'Open Sans', 'Helvetica Neue', 'Segoe UI', 'Calibri', 'Arial', sans-serif;
        font-size: 18px;
        color: #60666d;
    }
`

export default StyledSelect;